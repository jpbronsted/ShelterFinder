<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="team.gatech.edu.login">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">
        <activity android:name="controllers.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="controllers.QueryResultsActivity" />
        <activity android:name="controllers.LoginActivity" />
        <activity android:name="controllers.RegisterActivity" />
        <activity android:name="controllers.DetailViewActivity" />
        <activity android:name="controllers.SearchActivity" />
        <activity android:name="controllers.MapActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"
            />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAlgv-zbj2pgcS5nv76kc-bpuJcHo9-xK0"
            />
        <meta-data
            tools:replace="android:value"
            android:name="com.google.android.gms.version"
            android:value="com.google.android.gms:play-services-maps:12.0.1"
            />
    </application>

</manifest>